package models

data class Chapters (var number: Int,
                     var chapterTitle: String,
                     var numPages: Int,
                     var numWords: Int,
                     var includesPictures: Boolean
    ) {

}